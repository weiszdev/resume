# Resume — Jonathan C Weisz

Live: **https://weiszdev.github.io/JCWRes/**

| File | Purpose |
|------|---------|
| `resume.md` | Source of truth — plain markdown, easy to edit |
| `index.html` | The published page (also the print/PDF version: Cmd+P → Save as PDF) |

`index.html` is self-contained: inline CSS, light/dark aware, responsive, and print
styles that drop to a clean 0.4in-margin page. Fonts load from Google Fonts with
system fallbacks.

Contact details are intentionally omitted from this public copy.

Edit `resume.md` first, then mirror the change into `index.html`.
